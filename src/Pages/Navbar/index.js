import Profile from '../Profile';
import ChatList from '../ChatList';

function index(props) {
  return (
    <div>
      <h1>nav area</h1>
      <Profile />
      <ChatList />
    </div>
  );
}

export default index;
